/*
Пользователь вводит последовательность чисел, в которой каждое число лежит в диапазоне [0,10000].
Ввод чисел заканчивается при вводе числа -1. Требуется найти максимум среди чётных чисел.
*/

#include <stdio.h>

int main()
{
    int num, max=0;
do {
    scanf ("%d", &num);
    if (num%2==0 && num>max)
    max=num;
    }
while (num !=-1);
printf ("%d", max);
    return 0;
}
